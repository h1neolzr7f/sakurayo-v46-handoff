# 负责任使用

樱夜·尸潮只做一件事：在本机离线游玩一局原创肉鸽射击，并把进度写回本地存档。

## 推荐用法

1. 从 [本仓 Releases v4.6.4](https://github.com/h1neolzr7f/sakurayo-v46-handoff/releases/tag/v4.6.4) 下载「测试版樱夜」APK，或克隆后打开 `src/index.html`。公开玩家仓仍停在 v4.4.6。测试包不会覆盖正式包。
2. 用发布说明里的 SHA-256 核对安装包。
3. 在本机游玩。存档键是 `sakurayoV3`，升级后缺字段会自动补齐。

## 不要这样做

- 不要从来路不明的网盘安装「解锁版」「内购版」
- 不要把签名密钥、`keystore.properties` 或完整私人存档贴到公开 Issue / PR
- 不要清空 `localStorage` / 应用数据来「修」旧档；缺字段应当由游戏补齐
- 不要把商店改成直接出售永久伤害，或把存档键改掉导致旧档读不出来
- 不要把本仓库美术和角色设定拿去冒充其他商业作品的官方资源包

## 数据放在哪

- 浏览器：本机 `localStorage`，键名 `sakurayoV3`
- Android：WebView 同源存储，覆盖安装正式签名包时会保留

发行包和 git 仓库都不包含你的存档。
