# AGENTS.md — Codex 开发约束

接手请先读根目录 [README.md](README.md) 和 [docs/HANDOFF.md](docs/HANDOFF.md)，再改代码。一页清单见 [README_FIRST.md](README_FIRST.md)。

## 项目目标

继续开发手机端离线二次元肉鸽射击游戏《樱夜·尸潮》。以 `src/index.html` 为唯一代码基线，最终发布仍需生成单文件 HTML。

## 不可破坏的功能

- 三名角色：月城小夜（步枪远程）、神代绫（手枪＋太刀）、黑羽凛音（纯太刀）。
- 14 个基础职业、28 条转职分支、18 种跨职业融合、科技/生物/灵能三相飞升。
- 四章剧情、Boss 四阶段、构筑稳定度、克制怪、吐槽电台、衣装商店、成就和本地存档。
- 角色、怪物、掉落、子弹、场地危险和 UI 必须正常绘制。
- 手机触控摇杆、冲刺和主动技能必须可用。

## 强制兼容要求

- 继续使用 localStorage 键 `sakurayoV3`。
- 旧存档缺少新字段时自动补齐，禁止清空玩家存档。
- 不依赖网络、CDN、外部字体或外部图片。
- Android WebView/Chrome 可直接打开本地 HTML。
- 避免只在桌面浏览器可用的 API；使用新 API 时必须提供回退。

## UI 信息优先级

从高到低：

1. 剧情 / 抉择 / 升级 / 暂停 / 结算模态层
2. Boss 转阶段和场地危险警告
3. 顶部系统提示
4. 底部吐槽电台

模态层出现时暂停并收起其他文本；退出模态层后按队列继续，禁止文字重叠挡住操作。

装修、重做界面时：先按 [docs/ART_BIBLE.md](docs/ART_BIBLE.md) 第 6 节和现有大厅/战斗截图文生图出稿，再 CSS 复刻。保持樱粉/暖金/深紫/细金线这一套，不要另起风格，也不要用全员缩小冒充适配。

## 性能红线

- 不允许恢复“每颗子弹遍历全部敌人”的无界碰撞。
- 敌人、子弹、Boss 弹、召唤物、伤害文字和粒子都必须有上限。
- Boss 阶段不允许无限召唤普通怪。
- 避免每帧创建大量临时对象或执行 JSON 深拷贝。
- 目标：430×932 视口下中端安卓可持续运行。

## 代码修改规则

- 优先小步修改；每个功能完成后检查启动、升级、Boss、结算和重新开局。
- 不得删除或改名核心函数而不更新调用点，尤其是 `startGame()`、`update()`、`draw()`、`spawnEnemy()`、`showDialogue()`。
- 新职业/融合必须同时定义：触发条件、攻击循环、强势期、弱势怪、明确代价、吐槽文本、结算显示。
- 商店皮肤只影响外观和职业出现倾向，不直接出售永久伤害。
- 剧情选项要改变至少一项：关系、Boss 行为、属性、档案或结局判定。

Git 仓库根目录就是本文件夹。升版本、验证命令和 tag 约定见 `docs/MAINTAIN.md`。

## 每次交付前至少验证

1. HTML 内联 JavaScript 语法检查通过。
2. 新存档与缺字段旧存档都可进入主菜单。
3. 三个角色都可开局，并在 10 秒内看到角色、怪物和攻击。
4. 至少完成一次升级选择，选择后战斗继续。
5. Boss 依次进入 75%、50%、25% 三个转阶段节点。
6. 剧情和吐槽不会同时遮挡屏幕。
7. 死亡/通关结算可关闭并重新开局。
8. 发布文件同步到 `release/`，并更新变更记录。
