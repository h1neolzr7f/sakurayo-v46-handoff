# 樱夜·尸潮 V3.5 Codex 续作计划

## 目标
只以 `src/index.html` 为代码基线，先完成 CODEX_TASK 第一目标中的可靠基线建设：可启动、可重复冒烟、清晰区块、开发期全局错误显示；随后按证据选择一个最小内容垂直切片。发布仍为离线单文件 HTML，并保持 `sakurayoV3` 兼容。

## 阶段
- [x] 阶段 1：校验交接包、在桌面安全解压、读取根协议和指定设计文档
- [x] 阶段 2：并行审计源码、现有工具/测试、需求缺口
- [x] 阶段 3：建立/修复可重复冒烟测试与开发错误显示
- [x] 阶段 4：完成可靠基线并将内容扩展留给后续独立阶段，避免未测试的大改
- [x] 阶段 5：执行静态与浏览器回归，检查新/旧存档、三角色、升级、Boss、结算
- [x] 阶段 6：同步下一版本 release，更新 CHANGELOG、测试记录、已知问题、case state 与证据报告

## 硬约束
- 代码只改 `src/index.html`；不得从 `release/` 或任何旧版文件回填。
- 保留 `localStorage` 键 `sakurayoV3`，缺字段旧存档自动迁移且不清空。
- 无网络/CDN/外部字体/外部图片；Android WebView/Chrome 本地可运行。
- 保持实体上限和有界碰撞；不删除核心函数调用契约。
- 未实际执行的测试不得声称通过。

## 已知异常/错误
| 项目 | 尝试 | 处理 |
|---|---:|---|
| 外层 ZIP SHA-256 与提供的校验文件不一致 | 2 | 搜索同目录无替代副本；逐条目解压读取检查 0 损坏，记录风险后隔离解压 |
| 首次读取 docs 的 PowerShell 字符串 `$name:` 解析失败 | 1 | 改为 `${name}:` 后成功 |
| ZIP 中文条目名默认解码乱码 | 1 | 用 ZipArchive UTF-8 `entryNameEncoding` 解码并做路径穿越校验 |

| 最终冒烟首次重跑时随机升级导致角色场景进入 level | 1 | 新增测试专用 freezeProgression()；随后完整 runner 连续 3 次通过 |
| PowerShell 直接给 PSCustomObject 新增 test_repeats 属性失败 | 1 | 改用 Add-Member -Force 后成功 |
| 首次生成文档时 PowerShell 双引号 here-string 将 Markdown 反引号解释为控制字符 | 1 | 改用单引号 here-string + 占位符替换，扫描 7 个文档控制字符为 0 |
| 本轮恢复脚本编排中的 JavaScript/PowerShell 路径字符串转义失败 | 2 | 第三次改为不含 PowerShell 反引号的固定双引号路径，成功恢复并读取 case state |
| 本轮首次更新计划时组合补丁上下文校验失败 | 1 | 拆分为小范围精确补丁后成功 |
| 不透明导航图标生成 QA 联系表时 RGB 与 RGBA 模式不匹配 | 1 | 预览缩略图统一转 RGBA 后再 alpha_composite，并重跑该任务 |
| develop-web-game 客户端不支持 `--help`，首次帮助探测缺少 `--url` | 1 | 直接读取脚本参数解析段，随后使用完整 URL、actions-file 与 screenshot-dir 成功运行 |
| aapt 在中文 APK 路径上报 Illegal byte sequence，PowerShell 首次未加载 ZipFile 程序集 | 1 | 将同一 APK 复制到 ASCII 输出路径，并加载 System.IO.Compression.FileSystem 后完成权限与内容验证 |
| 首次改写旧 SHA 文件的上下文校验失败 | 1 | 删除带旧编码/BOM 的文件并用 apply_patch 重建为 UTF-8 文本 |
| planning-with-files-zh 的 check-complete.ps1 因技能文件自身乱码无法解析 | 1 | 不修改技能；改用计划复选框、JSON 解析、产物哈希和测试报告做手工完成门禁 |
| V3.7 恢复时 PowerShell 对 `(Get-Command python).Source` 的调用表达式无效 | 1 | 不重复该写法；改用 `python` 命令或先保存可执行路径变量再调用 |
| V3.7 首轮回归的旧存档深比较缺少新增主神字段 | 1 | 更新迁移断言，显式验证 deepest/contracts/challenges 默认值 |
| 无限回廊测试读取到 `mainGod-4` 而非层数键 | 1 | 战斗逻辑正确；修正测试快照直接使用 `stage().key`，让文本状态与实际关卡一致 |
| 教程回归后统计按钮的 Playwright 普通点击等待动作稳定性超时 | 1 | 截图与可见性确认按钮正常；测试改用 force click，继续验证真实点击处理器和统计抽屉结果 |
| branding Image2 首次请求网络错误 | 1 | 未产生项目文件；保留已完成的四张 UI 图集请求，稍后减少参考图组合并重试品牌图 |
| Android 构建命令首次在项目根目录调用 `gradlew.bat` | 1 | 切换到 `android-app` 后使用 `--no-daemon` 完整构建成功 |
| PowerShell 直接重定向 `adb exec-out screencap` 产生损坏 PNG | 1 | 改为设备端 `screencap` 后 `adb pull`，模拟器截图可正常解码 |
| V4.0 同步后在项目根调用 `gradlew.bat` | 1 | 同步成功；构建改为切换 `android-app` 工作目录后执行，不重复错误路径 |
| 隐藏后门首次测试时封面副标题被菜单层拦截点击 | 1 | 为副标题显式启用 pointer-events、touch-action 和前景层级后重新回归 |
| V3.8 敌人类型审计的双引号正则含 `|`，被 PowerShell 当作管道解析 | 1 | 不重复该命令；改用单引号正则或分段读取源码 |

## 本轮：Image2 全量美术与 Android APK

目标：以用户参考图为风格基准，用 gpt-image-2 生成并抠图游戏全量素材，改为 APK 内置资源；游戏只需在 Android APK 中离线运行，不再要求单文件网页交付。

- [x] 阶段 7：Image2 可用性检查、全量素材盘点、规格与提示词
- [x] 阶段 8：使用当前可用的内置 image_gen（Image2）生成角色/UI 核心素材，完成透明抠图、接入与像素验证
- [x] 阶段 9：建立 Android WebView 原生壳并构建可安装 APK
- [x] 阶段 10：将生成素材接入游戏与 APK，执行设备尺寸、存档、触控和性能回归

## 本轮：补齐剩余 UI

- [x] 阶段 11：审计剩余 UI 清单、实际调用点与 Android 品牌资源
- [x] 阶段 12：生成并切分职业/转职/融合/飞升、升级技能和品牌图集
- [x] 阶段 13：接入职业图鉴、升级卡、成就/加载/应用图标并保留回退
- [x] 阶段 14：浏览器截图、交互、资源解码和性能回归
- [x] 阶段 15：Android 构建、模拟器安装、视觉验收和交付同步

## 下一轮：角色动态表现与服饰

- [x] 阶段 16：锁定三角色参考图风格与动画状态规格
- [x] 阶段 17：制作待机/移动/攻击/受击/冲刺关键帧并接入方向状态机
- [ ] 阶段 18：完成 8 套服饰 × 3 角色的差异化外观并接入商店/战斗
- [ ] 阶段 19：移动端动画性能、身份一致性和全流程回归

## 本轮：Q 版战斗比例、碰撞与成熟度对标

- [x] 阶段 20：审计当前屏占比/碰撞/剧情节奏，并形成成熟移动肉鸽对标基准
- [x] 阶段 21：为三角色生成并切分 Q 版待机/移动/攻击/受击/冲刺关键帧
- [x] 阶段 22：接入脚底锚点、方向动画、视觉尺寸与独立碰撞半径
- [x] 阶段 23：补齐剧情与可玩性门禁，验证角色专属事件、吐槽、升级与 Boss 节奏
- [x] 阶段 24：浏览器与 Android 长流程回归、截图验收、构建和 APK 同步

### 新约束
- 不使用逆向实验室。
- 美术生成使用当前会话可用的内置 image_gen（用户称 Image2）；不依赖本机 API key。
- 需透明的素材先生成纯色键控背景，再用本地脚本抠图并验证 alpha、边缘和裁切。
- APK 资源允许拆文件，不再强求全部内联到 HTML。

## 本轮：自由武器方向与主神空间

- [x] 阶段 25：审计自由瞄准、定向挥砍、角色吐槽和高难成长接入点
- [x] 阶段 26：实现移动/瞄准解耦、360°武器表现和扇形刀伤
- [x] 阶段 27：实现主神空间副本、奖励点与副本内永久强化
- [x] 阶段 28：为三角色接入各 8 个触发型彩蛋和主神空间剧情
- [x] 阶段 29：扩充自动测试并完成浏览器、Android、APK 交付回归

### 本轮约束
- 主神空间使用原创的镜界轮回监管层设定，只借鉴高层概念，不照搬具体角色或桥段。
- 奖励点强化只在主神空间生效并跨该副本局次保存，不污染普通四章平衡。
- 移动向量与瞄准向量必须独立；刀剑伤害必须同时通过距离和夹角判定。

## 本轮：V3.7 主神轮回深化与正式化

目标：停止扩充职业数量，完成主神长期循环、正式发布能力、真实服饰素材、Boss独立演出、声音触感、新手引导、职业树和可用于平衡的数据闭环。

- [x] 阶段 30：冻结 V3.7 规格，审计存档、发布、主神经济、服饰资源、Boss表现、音频和引导接入点
- [x] 阶段 31：实现主神多级兑换、随机契约、挑战目标、无限层与强化重置
- [x] 阶段 32：实现新手引导、职业树总览、推荐原因、冲突提示和完整局后数据/导出
- [x] 阶段 33：实现 Boss 独立视觉演出、场地身份、音频层次、震动与设置
- [x] 阶段 34：使用 Image2 完成 8 套服饰 × 3 角色的头像、对话与 Q 版五状态素材，接入商店和战斗
- [x] 阶段 35：拆分高风险数据/系统边界并保持 `src/index.html` 发布入口、旧存档和离线运行兼容
- [x] 阶段 36：完成浏览器全流程、Android 性能、旧存档迁移、正式签名 Release 构建与最终交付

### V3.7 完成门禁
- 主神兑换可多级升级、重置返还；随机契约和挑战目标会实际改变战斗与奖励。
- 三名角色的 8 套商店服饰均有真实身份一致素材，不再只换配色。
- 四个普通 Boss 与主神监察者拥有可辨认的独立阶段表现和预警。
- 第一局可完成教程；职业树可查看前置、分支、融合与飞升；结算可导出本地统计。
- 音量、震动和特效强度可设置；普通章节、主神空间和旧存档均不互相污染。
- 交付为可安装 Release APK；若本机缺少用户正式密钥，则生成并保管项目专用签名并明确记录后续升级必须沿用。

## 本轮：V3.8 战斗表现与叙事重制

目标：停止增加职业数量，优先消除“宣传立绘与战斗占位画面”的品质断层，重构手机战斗信息层级，并让三名角色拥有不同的章节冲突、选择代价与结局回收。

- [x] 阶段 37：审计 HUD、升级、职业树、商店、主神大厅、敌人/Boss 美术和三角色剧情数据，冻结 V3.8 垂直切片
- [x] 阶段 38：重构战斗 HUD、Boss 屏外指示、场地预警、角色/敌人对比度和吐槽可读性
- [x] 阶段 39：重构升级三选一、职业树、服饰预览、主神分类与存档导入导出 UI
- [x] 阶段 40：重写三角色四章主线、关键抉择、Boss 阶段对白、主神支线和结局回收
- [x] 阶段 41：使用 Image2 制作并接入 Q 版敌人、四章 Boss 与主神监察者素材，保留程序化回退
- [x] 阶段 42：完成浏览器、旧存档、剧情分支、Android 性能、正式签名 Release 构建与交付

### V3.8 完成门禁
- HUD 在 360×800、430×932 与横屏均不遮挡主要战斗区域；Boss 离屏时有方向和距离提示。
- 三选一直接显示数值变化、职业进度、推荐原因和冲突风险；职业树不再只是长篇说明列表。
- 三名角色的关键选择会设置不同 flag、改变后续对白并导向不同角色结局，不只替换姓名。
- 普通敌人和 Boss 使用真实 Q 版位图素材，加载失败时仍可用旧 Canvas 回退。
- 继续使用 `sakurayoV3`，普通章节与主神永久强化互不污染；APK 保持完全离线并沿用 V3.7 正式签名。
# V3.9 全面收敛与优化（2026-07-12）

- [x] 审计战斗、Boss、主神空间、UI、存档与性能基线
- [x] 加入完美闪避、近战格挡反馈与伤害数字降噪
- [x] 增加敌人冲锋/支援职责与四章 Boss 规则机制
- [x] 扩充主神永久兑换、诅咒遗物与轮回变体
- [x] 完成 UI 减法、剧情回顾、可访问性和玩家辨识
- [x] 强化存档校验、本地统计与后台恢复
- [x] 通过浏览器 30+ 回归、怪海压力与 Android 构建安装验证

验收口径：不以“文件存在”为完成；必须在 430×932 手机视口完成开局、武器方向、近战扇区、升级、Boss 阶段、主神商店、存档导入和怪海压力回归，并重新签名验证 APK。

## V4.0 内测轮回与商店构筑（2026-07-12）

- [x] 阶段 43：建立仅 beta/test 可见的内测后门、直达场景与本地数据采样
- [x] 阶段 44：扩充普通商店的初始核心、武器道具、丧尸诱饵与职业排除符咒
- [x] 阶段 45：接入逐章放大的初始强化、纯初始挑战与隐藏成就/吐槽/剧情
- [x] 阶段 46：加入有明显代价的冲突升级，并在升级卡与角色吐槽中明确反馈
- [x] 阶段 47：运行无局内升级通关、职业排除、诱饵压力、存档迁移与完整浏览器回归
- [x] 阶段 48：依据测试数据校准数值，构建 Android 4.0 正式包并安装验证
- [x] 阶段 49：新增 6 个跨职业融合并接入独立战斗循环、代价和天敌
- [x] 阶段 50：补齐新融合图鉴、图标回退、专属吐槽及商店道具/排除符边界
- [x] 阶段 51：扩充融合自动测试，重新执行完整浏览器与 Android 4.0 发布验证

### V4.0 完成门禁

- 正式模式不暴露内测操作；`?beta=1` 或测试模式可直达角色、章节、时间、Boss、资源和报告。
- 商店道具不直接混入服饰列表，清楚显示永久购买、装备状态、章节倍率和局内/跨局边界。
- 职业排除符最多按进度开放 1–2 个槽，不能排除通用生存项或导致升级池为空。
- 丧尸诱饵增加刷怪频次、普通波次持续时间和经验收益，但仍遵守动态实体上限。
- 终章纯初始挑战在“无伤/完美走位代理”测试下可实际完成，不靠测试秒杀 Boss。
- 新存档字段继续沿用 `sakurayoV3` 并通过旧存档迁移。

## V4.1 可扩展框架（2026-07-13）

目标：不重写战斗循环，通过深模块接口建立内容包注册、资源回退、存档扩展迁移、失败隔离和可重复构建；开发态允许多文件，发布态仍生成离线单入口。

- [x] 阶段 52：恢复上下文、审计单体入口/Android 同步/存档与内容接入点，冻结渐进式架构
- [x] 阶段 53：实现经典脚本兼容的运行时注册表、事件接口、资源解析与扩展故障隔离
- [x] 阶段 54：在不改战斗循环的前提下接入存档、服饰、成就、剧情/文案扩展适配器
- [x] 阶段 55：加入官方示例扩展与故意损坏扩展夹具，证明正常追加与失败隔离
- [x] 阶段 55A：将 Image2 四章剧情地图作为第二个官方包接入，完成移动、拾取和独立持久化
- [x] 阶段 56：实现开发入口、单文件构建、静态检查和 Android 同步新流程
- [x] 阶段 57：执行旧档、坏扩展、完整浏览器冒烟、离线资源与 Android 构建安装验证
- [x] 阶段 58：完成 ARCHITECTURE、扩展说明、CHANGELOG 和验证报告，重建公开无密钥交付

### V4.1 完成门禁

- `sakurayoV3` 键不变，缺字段与扩展自有存档默认值均可合并；损坏 JSON 回退默认档而不清除原值。
- 正式模式不创建测试后门；`?test=1`/`?beta=1` 保留但与内容包接口分离。
- 单个扩展注册、迁移或内容应用失败时记录扩展错误并继续启动本体。
- 官方示例扩展至少追加一件服饰和一项成就，且无需编辑战斗循环。
- 开发态多文件与 Android/发布单入口由同一构建清单产生，资源完全离线且图片失败保留回退。
- 现有 39 项浏览器冒烟与新增框架测试均通过，Android 同步脚本验证最终构建产物而非直接复制旧单体。

### V4.1 本轮错误记录

| 错误 | 尝试 | 处理 |
|---|---:|---|
| 组合补丁未命中单体文件精确上下文 | 1 | 拆成运行时、内容适配器和资源路径小补丁后成功 |
| `Copy-Item -LiteralPath` 不展开服饰资源通配符 | 1 | 改用 `Get-ChildItem -File | Copy-Item`，复制 21 个文件 |
| WinGet 的 ffmpeg 链接存在但不能启动 | 1 | 改用已安装 Pillow 将四张 PNG 压缩为 WebP |
| 框架测试仍只期望一个官方包 | 1 | 更新为示例包与探索包，并加入探索流程断言 |
| 图片在异步解码完成前被立即断言 | 1 | 等待 `complete && naturalWidth > 0` 后验证 |
