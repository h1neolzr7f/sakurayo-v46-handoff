# 《樱夜·尸潮》V4.2 离线版

用 Chrome、Edge 等浏览器打开 `index.html` 即可。部分手机浏览器禁止直接读取本地文件；这种情况下请使用同包 APK，或在电脑目录运行：

```powershell
python -m http.server 8765 --bind 127.0.0.1
```

然后访问 `http://127.0.0.1:8765/`。

- 不需要 CDN 或网络资源。
- 存档键仍为 `sakurayoV3`。
- 正式入口不会显示内测后门。
- 扩展状态在主菜单“更多设置与数据 → 扩展包”查看。
