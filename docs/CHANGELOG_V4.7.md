# V4.7.3 — 立绘完整、寻访叠伤可见、符咒/邮箱精装

2026-08-19。开发版夜樱 `4.7.3-yeying` / versionCode **6113**。存档键仍是 `sakurayoV3`。没有再包 `update`。

- 大厅立绘呼吸层锁 `width:100%`，横屏 mask 收到 94%，脚底 fade 收到 6%，不再裁头裁身
- 寻访标题、抽卡揭示和大厅 `Lv.` 行写清当前火力与本抽叠伤；按钮改「单次叠伤 / 十连叠伤」
- 14 枚排除符走 `data-shop-ico=t*` 符纸 SVG；邮箱默认选中内测补给，礼物/信封图标不再用草稿框
- 夜樱内测补给置顶：🌸25000 · 券×80 · 碎片 800 · 棱晶 500，可直接领取
- 门禁：`style_lock_unit` / `regression_v47` 锁符咒映射、叠伤文案和补给数额
- 本机开发包：`release/YeYing-Dev-v4.7.3-android.apk`（SHA256 `D593B28F5B732D88054208EBC3D72803B5860D3E929F0EA66336DD95BDACA631`）

# V4.7.2 — 关掉误开后门，锁商店立绘图标

2026-08-18。开发版夜樱 `4.7.2-yeying` / versionCode **6112**。存档键仍是 `sakurayoV3`。没有再包 `update`。

- 开发包不再带 `?beta=1`，「更多」里不再出现内测后门按钮；游玩入口仍在大厅/邮箱/商店
- 立绘连点不再给玩家发币；点头点身只播动作
- 内测资源改走邮箱「夜樱内测补给」（樱花币 / 寻访券 / 碎片 / 棱晶）
- 商店立绘和轨/邮箱图标先占最终尺寸，晚到的橱窗/精装脚本不再把卡片和图标撑大
- 寻访标题写清入册叠伤；排除符按职业换图标
- 接着把同类跳变清完：模式图标 22px、商店侧栏 44px、名册立绘 118px、寻访立绘 52%、活动条 50×48；邮件格子不再从 42 收到 22
- 门禁：`tests/style_lock_unit.mjs` 锁源码互斥尺寸，`tests/layout_stable.mjs` 量晚到脚本注入后的实测盒子
- 本机开发包：`release/YeYing-Dev-v4.7.2-android.apk`（SHA256 `31CC1458321CDF71145F0C0A8F9E1F6D35CAC9235CB01CA68938A7699C3132DC`）

# V4.7.1 — 主神默认可进与叠伤收口

2026-08-18。开发版夜樱 `4.7.1-yeying` / versionCode **6111**。存档键仍是 `sakurayoV3`。没有再包 `update`。

- 大厅回收 / 证词 / 主神三丸始终可见；`mainGodOpen36() => true`，不再看第 4 章，也不把第 4 章写进 `save.done`
- 寻访入册叠伤、核心/弹匣加伤保留；档案房间补回「成就图鉴」
- 横屏顶栏露出棱晶/碎片；竖屏成就轨图标给固定尺寸
- 虚空怪只画已有静帧，不再请求 `voidling_b` / `voidmaw_b`
- 局外壳提示改为「本地系统 · 不联网」
- 本机开发包：`release/YeYing-Dev-v4.7.1-android.apk`

# V4.7.0 — 统一夜樱修复线与大厅精装

2026-08-18。开发版夜樱 `4.7.0-yeying` / versionCode **6110**。存档键仍是 `sakurayoV3`。没有再包 `update`。

把两条并行对话收成一版：

- `bc-96d1ebb8-2301-42a1-816a-b62772e3eaa0`：触控穿透、口令观察者卡死、简报挡住暂停、橱窗刷新崩大厅、6109 图标精装
- `bc-6b2125dd-b4ac-4d6d-8176-41d6f835cadf`：寻访仪式 / 换角身份 / 电台让路 / 结算钩子，以及顶栏换角压货币条

## 合并进来的能力

- 横屏大厅、16 卡寻访、证词、主神、局内 2 干员 DP、仿 Live2D、作战简报、四阶段机制条、分层打击反馈
- feel53：进大厅 3 秒「本期概率提升」、换角身份色/口令/琵音、电台 2.2 秒收起、结算钩子不发奖励
- touch54：UI 层拦住落到画布上的点击，简报时藏 HUD，避免暂停成死按钮
- layout52：Android 锁横屏后不再误走竖屏回退；换角圆钮挂进顶栏 flex，藏预览 prism/shard，日历/支援留缝，「更多」左留 10px
- boutique：时装商店列出全部衣装；`busy` 门禁防止刷新重入崩大厅
- chrome6109：大厅到抽屉、成就、教程、天赋、飞升、事件、结算、抉择的草稿 emoji 换成精装图标；`dress` 节流，不再每 90ms 重写整页 CSS（手机点教程/出击会抖）
- 手机横屏藏 `.utilityButtons37`，冲刺/技能最小 52px；开发版 Debug 开 `?beta=1` 方便实机过 Boss 节点
- `persist` 不再在 `menuUpdate` 里重入；测试模式作战简报立刻回调，避免卡在 briefing 层
- 对白/战斗里 `persist` 不再整页重建大厅；隐藏抽屉不重绘；出击键就地改字
- `resize` 带 `busy`，画布尺寸没变不重设 `canvas.width`，避免量热区卡死
- 吐槽/过场不再用 `offsetWidth` 强制回流；口令观察者同一帧最多脉冲 6 次
- 进战斗电台不再对 `#banter` 反复加 `live53` 卡死主线程
- 手机横屏出击不再切掉四角热区；412px 高藏问候条和口令，避免被看成关不掉的签到层
- 暂停/结算次要按钮改实心底与金边；矮横屏暂停不铺闪图
- 过场切入不再从透明起势，暂停/结算不会只剩一层模糊光圈
- `finish` 缺 `damageSources` 时不再抛错卡结算
- 412px 横屏藏左下寻访条；升级「重抽」、寻访「跳过」、结算诊断改实心底；矮屏结算不再铺闪图
- 寻访单抽/十连在 430px 高取消斜切，收下证词卡压到 168px，避免撑破视口
- 寻访「跳过」在 412px 高保底 72×42，不再被压缩 padding 收到 62px
- 升级/暂停/结算出现时强制收起吐槽；矮横屏藏画质角标，减轻战斗 HUD
- 412px 横屏升级卡压缩标题、说明和读出，重抽整颗留在视口内
- 测试模式立刻摘掉启动层；正式局 900ms 内收起，不再用半透明「初始化中」盖住大厅和升级卡
- 关卡简介卡不再抢「更多 / 统计」点击；底栏图标走 chrome SVG，不再和 webp 互相覆盖
- 预发布侧载包：`YeYing-Dev-v4.7.0-android.apk`（Debug，`com.sakurayo.yeying.dev`，对应 `884d7c5`）

## 明确不再犯

见 [REGRESSION.md](REGRESSION.md)。口令观察者必须带 `busy` 且只听 `childList`；橱窗装饰必须带 `busy`；换角不得再绝对钉死压货币条；干员不进 `pets`；不清档、不改存档键。

## 未并入

主神货架精装修仍在独立分支 `cursor/maingod-shop-volume1-3dfe`（PR #4），这次没有混进统一版，避免三线冲突把已修 bug 冲掉。

公开玩家仓仍是 v4.4.6。本包是开发版夜樱侧载，不会覆盖正式《樱夜·尸潮》。
