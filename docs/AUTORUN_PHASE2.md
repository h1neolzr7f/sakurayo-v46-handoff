# 第二期开工单（28 转职进残片池）

完整提示词：`docs/CURSOR_PROMPT_PHASE2.md`。不要和 `CURSOR_PROMPT_P2.md`（严重度 P2）混用。

## 基线

- `origin/main` @ `491e548`（第一期已合并）
- 新分支 `cursor/phase2-promos-5030`
- 版本仍 **4.6.0**，存档键仍 `sakurayoV3`

## 人只露脸 3 次

1. **开工**：把 `CURSOR_PROMPT_PHASE2.md` 围栏全文贴进新项目。
2. **认人**：28 张 `job_*.webp`。回「过 / 哪几张重画」。超过 24h 按自检继续。
3. **收工**：门 A 绿、残片仍 R 多于 SR、硬保不是残件。你点合并。不要升 4.6.1。

## 已锁死（开工后不准改）

- 28 张全是 SR，进残片池；N/R 滚动仍落到 R
- 硬保/软保 want=SSR → SR 桶，优先未拥有，禁止残件；履约清 pity
- 拥有：该校倾向 ×1.5，全伤害 +0.5%；无转职套装
- 融合仍不进池

## Agent 四刀

0 摸底 → 1 数据+仓库页 → 2 并行出图 → 3 加成与回归 → 4 清虫停
