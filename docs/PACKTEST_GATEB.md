# 打包测试门 B / 门 C

2026-08-19。最多 5 轮。连续两轮无新 P0/P1 后停。版本仍 4.6.0。

优先类：镜头、编年、寻访/仓库、存档、核心循环、触控、性能。

## 轮 1

扫刚并进 main 的镜头 / 编年 / 三页寻访。写 `tests/packtest_gatec.mjs` 用测试 API 跑门 C 剧本。

| 类 | 复现？ | 处理 |
|---|---|---|
| 镜头 follow/snap 没夹边 | 否。`camera_unit` 夹边；活局走到左缘 `camX≈0` | 不改 |
| 怪刷在世界尽头 | 否。`spawnFromEdge50.inside === false` | 不改 |
| HUD 被 translate 拖走 | 否。`#hud` 是 `position:fixed`，夹边后 left≈0 | 不改 |
| 10 秒看不见战斗 | 否。三角色 10 秒内有人/怪/攻击 | 不改 |
| 编年点回空墙 / 小夜五条被冲 / 顶栏「三角色」 | 否。13 卡 + 三段标题；点回残件 8 格 | 不改 |
| 寻访三页切不过 / 旧档进不了菜单 | 否 | 不改 |
| 存档清档 / 新顶层 key | 否。键仍 `sakurayoV3`，缺字段只补齐 | 不改 |
| 升级卡死 / Boss 不转 / 结算关不掉 / update 再包一层 | 否。`update(dt)` 仍 `return fixedUpdate(dt)` | 不改 |
| 摇杆/冲刺/技能失灵 | 否 | 不改 |
| 子弹全量扫 / Boss 无限招普通怪 | 否。子弹走 `grid.near`；Boss 时 `enemies.length < 18` | 不改 |

首跑门 C 两次红都是**剧本写法**，不是玩法洞：新档 `start()` 先出教程；夹边 `camX` 浮点尘 `1e-31`。已写进剧本，未改 `src/`。

P2 见 `docs/PACKTEST_P2.md`。

## 轮 2

上一轮为什么漏：没有玩法洞可漏。差点把教程门和浮点尘当成 P1。本轮把「跳过教程」和「夹边 0.01 容差」写进 `packtest_gatec.mjs`，并重跑门 A + 门 C。

门 A 能跑的全绿。门 C 13 项绿。无新 P0/P1。

连续两轮无新洞，进入刀 5。不开刀 6。
