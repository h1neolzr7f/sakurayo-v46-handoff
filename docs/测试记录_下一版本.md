# 《樱夜·尸潮》下一版本测试记录

## 4.6.2 全玩法/画面补测（2026-08-20）

- **没有**把 14 职 × 28 转职 × 18 融合每条都打完一遍。测的是可进的房间、三种出击、三角色、升级、Boss 转阶段、干员、飞升/融合注入、三四章开场、隐藏关入口、探索、商店衣图、结算。
- **P0 画面已修：** Chrome 83 上 `.menu{inset:0}` 失效，操作台叠在立绘左边。补四边 + `left:auto;right:0` 后：立绘左 410px，操作台 x=422，出击和三角色钮都在右侧。
- 寻访 / 仓库 / 商店 / 关卡 / 档案 / 天赋 / 剧情 / 飞升 / 成就抽屉都铺满 640×360。商店 11 张衣图能加载。关卡三模式在。探索入口 5 个，探索层铺满，摇杆在视口内。
- 第二章可部署绫+凛音（不能部署自己），DP 坞在战斗中显示。融合 gunshrine/soulgun/nanoninja、飞升 tech/bio/psi 可写入。第三章开场「钥匙留在口袋」，第四章「先问她想不想留下」，Boss 都能进阶段 2。主神 HUD「主神空间 T1」。证词关干员坞。隐藏关 `enterHiddenArena47` 能进。
- 转阶段对话时电台收起；关掉后电台回来。失败「晚安，小夜」在卡内。
- 源图：`lobby_wide.webp` 左右不是整列黑边；三角色 `live_idle` / `battle` 有透明通道、无绿幕。
- 单文件 SHA-256 `f482ceab796604270f341664e4209a5512e46cc48d52ce9f4bf79471d2023608`。
- Debug APK SHA-256 `b037094592244350dfa7e86a69718382b6b02a4ce694dc830e0400f9ec399443`。
- 未升 4.6.3。未打正式包。

## 4.6.2 模拟器复测续（2026-08-20）

- 续用 Android 30 AOSP ATD、软件模拟、无窗口。WebView UA `Chrome/83.0.4103.120`，视口 640×360，dpr 1.5。
- **P0 已修并复测：** `#hud` 只写 `inset:0` 时 Chrome 83 不认，HUD 收成 0×0，`#joy` y=-134、`#dash` x=-157。补 `top/right/bottom/left` 后：`#hud` 640×360；摇杆 (18,226) 118²；冲刺 (483,287) 57²；技能 (540,262) 82²；暂停 (589,82) 41²。全部在视口内。
- 暂停 / 升级 / 寻访 / 商店 / 大厅 / 寻访揭晓 / 结算层都铺满 640×360。
- `?test=1`：点冲刺冷却 0→3.05，点技能 0→6.85，画布从摇杆位置拖动能位移（1376→1482）。离开测试页后 `adb` 点「出击」能进战斗，HUD 控件仍在视口内。
- 第二章 HUD「雨夜商圈」，Boss 75/50/25 → 阶段 2/3/4。买初始核心扣币，`tal.atk` 仍为 1。失败结算铺满，「晚安，小夜」在卡内，对话层和电台收起。
- 存档 `sakurayoV3` 重载后金币仍在（38545）。未升 4.6.3。未打正式包。
- 单文件 SHA-256 `112cbbcb7258f5f125f3b52df466593d773bcfa54a0b024215dcd5039bab4fed`。
- Debug APK SHA-256 `b5eca9cc09fdc56f547e1ccd82bc619ebaab09289f6e0cf3e1253e5d911a715f`。

## 4.6.2 模拟器复测（2026-08-19）

- 本机嵌套 KVM 不可用（`kvm_arch_vcpu_create` kernel BUG）。改用 Android 30 AOSP ATD、软件模拟、无窗口。
- API 34 Google APIs 在 TCG 下 system/systemui ANR，WebView renderer code 5 崩过一次。
- Debug 包 `4.6.2 / 63` 装上后：新档与缺字段旧档（`coins=7`、`tal.atk=1`）都能进大厅；ART_ROOT=`game/art`；立绘/导航图能加载；五房开关正常。
- 小夜 / 绫 / 凛音均可开局：10 秒内有人、怪、攻击；冲刺与主动技能冷却会走。
- 升级三选一后回战斗；Boss 在 75% / 50% / 25% 进入 2 / 3 / 4 阶段。
- **P1 已修：** 转阶段对话还在时调用 `finish()`，结算页会和 `#dialogue`、底部电台叠在一起。失败「晚安」只留在结算卡 `#resultRadio46`。
- **壳层：** WebView 启动前创建 `cache/WebView/Crashpad`，避免 log 里 mkdir 失败。
- 复测：转阶段对话未关时 `finish(false)`，结算只有结果页和卡内「晚安，小夜」，`#dialogue` / 底部电台都收起。回到大厅干净。
- 单文件 SHA-256 `e57e4e856b868d5b63e3a235f04bb2ddf0ec38fbd40d8b0512234cf4ab7c9281`。
- Debug APK SHA-256 `dd2575d6daabfb6af1f317fb0659fb4eb08aab533d3a72e36142c7b97e621a07`。
- 未升 4.6.3。未打正式包。

## 4.6.2 证词成就隔离与发版（2026-08-19）

- 证词通关后 `save.kills` / `runs` / `best` 不变，`checkAch()` 不跑，局内不点 `lv12` / `flaw` / `phasebreak`。章节、档案、结局、构筑卡仍不写。
- Chrome：证词结算隔离、开场不读演习回声、隐藏关标题用完美结局名。
- `python3 tools/static_check.py` + lobby / live / ops / chronicle / lifecycle / camera unit：通过。
- 版本 4.6.2 / versionCode 63。
- `assembleDebug lintDebug`：BUILD SUCCESSFUL。`aapt` 复核 versionName 4.6.2 / versionCode 63。
- 单文件 SHA-256 `b873d8d712cfddad93c242aa68a666838bd6c1238dd9a1b5c3375e962b924390`。
- Debug APK SHA-256 `e96a16323b2234f2c781d3e2c099114d7c56f04662d97abc35224e65b9364376`。无 `keystore.properties`，未打正式包。APK 与单文件在本机 `release/`（gitignore，不进仓）。

## 证词结算隔离 / 局内存档 / 文案对齐（2026-08-19）

- 证词通关标题为「证词已记下」，不写 `save.done` / `unlock` / `story` / `endings`，战绩 `mode=testimony`，`ending=null`，不触发孤证者。
- 演习已有 `rin=real` 时，证词第二章开场不会念「你信的是城外那个我」。
- 隐藏关打完后结算标题是「双生存续线 · 不是唯一也是真的」，并写入 `hiddenPerfect47.sayo`。
- `python3 tools/static_check.py` + 抽出 JS `node --check` + lobby / live / ops / chronicle / lifecycle / camera unit：通过。
- Chrome（系统 Chrome + puppeteer-core，`/src/index.html?test=1`）：上述三条全绿。
- 未跑完整 `browser_smoke.mjs`。未升 4.6.2，未交 APK / `release/`。没做「隐藏关死后直接再打」。

## 档案 / 证词分记 / 公开名单 / 旧结局层（2026-08-19）

- 档案回顾不再打印 `ayaSister：both`，改成八步人话。差 3 步以上不报数；差 1–2 步写「还差 N 步」；八步齐但前三章没通写「前三章还没走完」；全齐写「这些你都做了。终章会先把最坏的演完。」换角色会刷新同一张卡。
- 证词写入 `shop40.ops.story`，隐藏门闩只读 `storyChoices38`。`normalizeOps` 保留已有 `story.aya.ayaSister`。证词第四章不进隐藏关。
- 小夜公开名单（`identity=broadcast` / `P.publicTruth`）精英刷率约 +4%；跨章从演习记忆灌回。
- `_ending35` 只留 `localize35`。完美结局名未改。没有做「隐藏关死后直接再打隐藏 Boss」。
- `python3 tools/static_check.py` + `node --check` + lobby / chronicle / live / ops / lifecycle / camera unit：通过。
- Chrome（系统 Chrome + puppeteer-core，`/src/index.html?test=1`）：证词选 `ayaSignal=trace` 只进 `ops.story`，演习记忆和隐藏门闩都不吃；绫第四章未清隐藏关仍是写定结局，`setHiddenCleared47` 后是「姐妹归还线 · 救援必须可以拒绝」；档案人话会刷、差 1 步会报数、换角色会换清单。
- 未跑完整 `browser_smoke.mjs`（本机缺 Codex Playwright 路径）。未升 4.6.2，未交 APK / `release/`。

## 隐藏关状态泄漏（2026-08-19）

- 隐藏关里死亡或回大厅后，`P.hiddenArena47` / `P.hiddenCleared47` 不会随 `resetP()` 清掉。再开一局会把普通 Boss 加成成隐藏 Boss，打赢还可能直接记完美结局。
- 已在 `resetP()` 里清掉这三个局内标记。选项描述「稳定度 +8」与代码 +4 已对齐。绫专属「资产」电台不再对小夜/凛音播。

## 剧情节奏再收（2026-08-19）

- `python3 tools/static_check.py src/index.html` + `node --check`：通过。
- lobby / lifecycle / camera / ops / live / chronicle unit：通过。
- Chrome（系统 Chrome + puppeteer-core，`/src/index.html?test=1`）：
  - 第四章开场先落地人的损失，不再插「可预测的版本是什么 / 资产是什么」。
  - 第三章仍有凛锁定句「别急着证明谁是真的。先问她想不想活。」
  - 绫/凛音 16 个专属标题互不重复；选项描述不再出现「解锁隐藏 / 建立某某结局」。
  - 未清隐藏关时 `endingPreview` 仍是「写定的结局」；`setHiddenCleared47` 后绫为姐妹归还、小夜为双生存续。
- 未跑完整 `browser_smoke.mjs`（本机缺 Codex Playwright 路径）。未交 APK / `release/`。

## 终章突破宣泄口（2026-08-19）

- `python3 tools/static_check.py src/index.html` + `node --check`：通过。
- lobby / lifecycle / camera / ops / live unit：通过。
- 文案契约：凛「先问她想不想活」、结局名、选项名仍在。
- Chrome（系统 Chrome + puppeteer-core）第四章开场：小夜/绫/凛音均先念写死的悲剧，再拒绝。
- 完美结局改为隐藏关：未齐线索时 `endingPreview` 是「写定的结局」；八步+前三章通关后仍先给最坏结局；`setHiddenCleared47` 后才出双生存续。隐藏 Boss 血量约 1.55 倍。
- 未跑完整 `browser_smoke.mjs`（本机缺 Codex Playwright 路径）。未交 APK / `release/`。

测试日期：2026-07-10
测试源码 SHA-256：`0CE75E3EB81B1A922C9A7183E1FE6356ADD89C8E164CF59B7CAFA2335EEA3AEE`
目标视口：430×932；附加布局：360×800、932×430

## 实际执行命令

```powershell
python tools\static_check.py src\index.html
node --check tests\artifacts\static\index.extracted.js
node --check tests\browser_smoke.mjs
node tests\browser_smoke.mjs src\index.html
```

## 实际结果

- 静态核心符号、离线资源和内联脚本抽取：通过。
- Node.js 内联 JavaScript 语法检查：通过。
- 普通发布模式启动：通过；测试 API 未暴露。
- 360×800 与 932×430 主菜单：通过；无横向溢出。
- 缺字段旧存档：通过；`coins=7`、`tal.atk=1` 保留，其他字段补齐。
- 430×932 主菜单与三角色卡：通过。
- 月城小夜、神代绫、黑羽凛音：均可开局，7 秒确定性推进内出现敌人与实际攻击伤害。
- 触控摇杆、冲刺、主动技能：通过。
- 升级选择：通过；选择后返回战斗，等级和升级序列增加。
- Boss 75%、50%、25%：依次进入第 2、3、4 阶，阶段对白后恢复战斗。
- Boss 胜利结算与构筑报告：通过。
- 死亡结算、返回菜单、再次开局：通过。
- 核心流程 console/page error：0；外部 HTTP(S) 请求：0。
- 开发错误面板：同步异常与未处理 Promise 拒绝均可见。

自动测试共 15 项通过；修复随机升级导致的测试竞态后，完整 runner 连续 3 次通过。
报告：`tests/artifacts/smoke/report.json`。
截图：`tests/artifacts/smoke/*.png`。所有关键截图均已人工查看。

## 未验证

- 未执行 Android 真机 Chrome/WebView 测试。
- 未执行长时间真人平衡、内存和发热测试。
- 未覆盖全部 14 职业、28 分支、18 融合和三相飞升的组合排列。
## Android APK 实机环境替代验证（2026-07-10）

- Android 35 AVD：`TFModLab_API35`，1080×2400，density 420。
- `adb install -r release/樱夜尸潮_V3.5_Android_Debug.apk`：Success。
- `am start -W`：Status ok，LaunchState COLD，MainActivity 为 top resumed activity。
- 人工检查主菜单、角色卡、开场对话、7 秒战斗画面和触控输入后的事件界面。
- 玩家、敌人、子弹、HUD、摇杆、冲刺和主动技能按钮均可见；应用进程持续存在。
- logcat 未发现应用崩溃、AndroidRuntime fatal 或 Sakurayo/Chromium 页面脚本错误。
- 截图：`tests/artifacts/android/menu.png`、`play.png`、`battle.png`、`touch-controls.png`。
- 尚未在实体手机上验证不同厂商 WebView、刘海/挖孔和长期性能。

## Image2 角色与 UI 回归（2026-07-11）

- `static_check.py` 与抽取 JavaScript 的 `node --check`：通过。
- Playwright 冒烟：16/16，通过；新增 15 项本地 WebP 解码、尺寸和菜单资源断言。
- 430×932 全页截图：三角色卡、顶部头像、6 枚导航图标正确；三名角色战斗精灵和武器可辨。
- Android `assembleDebug lintDebug`：BUILD SUCCESSFUL。
- APK 内容：15 个 `assets/game/art/**/*.webp`；HTML 与源码哈希完全一致。
- APK v1/v2 签名、zipalign：通过；权限列表无 INTERNET。
- Android 35 `TFModLab_API35`：`adb install -r` Success，冷启动 Status ok，MainActivity top resumed。
- Android 冷启动约 14 秒后角色卡和导航图标完整显示；实际战斗截图中玩家精灵、HUD、摇杆、冲刺和技能键可见。
- 最终 APK SHA-256：`9017D2455B3C53DB1E48D56670B5B48D661565DCDF268CD3A6D60DB607AA8CA9`。
- 仍未覆盖实体手机长时间发热/内存与所有厂商 WebView；这不影响当前 debug APK 的模拟器可安装、可启动和核心流程验证结论。
