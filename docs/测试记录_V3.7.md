# V3.7 测试记录

测试日期：2026-07-12

## 自动检查

- `python tests/check_costume_assets.py`：通过，168 个 WebP、24 组服饰均可解码，尺寸 512×512，透明内容非空。
- `python tools/static_check.py src/index.html`：通过，13 个关键符号存在，内联脚本提取成功。
- 提取脚本与 `tests/browser_smoke.mjs` 的 `node --check`：通过。
- `node tests/browser_smoke.mjs src/index.html`：29 项通过。

浏览器覆盖旧存档迁移、三角色、24 组服饰切换、教程、职业搜索、统计、武器弹道共线、太刀扇形碰撞、三角色彩蛋、四章 Boss、主神契约/挑战/无限回廊、结算和无外网请求。

## Android

- `sync-game.ps1`：验证核心素材 15、剩余 UI 116、基础角色动画 15、服饰素材 168。
- `clean assembleRelease lintRelease`：BUILD SUCCESSFUL。
- APK：package `com.sakurayo.zombietide`，versionName `3.7.0`，versionCode `37`，minSdk 23，targetSdk 36。
- 签名：RSA 3072；v1、v2 验证通过；证书 SHA-256 `03A493A3447A507ABF407E1B66C1462AD8FA046028E73BA4F3E408A95B91A30B`。
- 模拟器：卸载旧测试签名版本后安装成功，主菜单与商店实机截图正常，角色/服饰图片能离线加载。

## 已知非阻塞项

- 新正式签名与此前 debug/旧临时签名不一致，首次安装正式版需要卸载旧测试包；从 V3.7 起后续正式版可正常覆盖升级。
- WebView 首次创建缓存时可能记录一次缓存索引重建日志，不影响游戏运行。
- 玩法平衡仍需要更多真人长局统计；V3.7 已提供本地 30 局数据导出用于后续调整。
