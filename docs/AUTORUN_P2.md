# P2 开工单（占位 + 换拼卡 + 补四段）

这是**严重度 P2**，不是圣经「第二期 28 转职」。

完整可粘贴提示词：`docs/CURSOR_PROMPT_P2.md`。

## 基线

- 代码：`cursor/autorun-phase1-d70b` @ `219eb4b`（或已合并 PR #10 的 `main`）
- 禁止从旧 `main` `9567de8` 开工
- 新分支：`cursor/p2-polish-5030`
- 版本仍 **4.6.0**，存档键仍 `sakurayoV3`

## 人只露脸 3 次

1. **开工**：把 `CURSOR_PROMPT_P2.md` 围栏全文贴进新项目。
2. **认人**：18 张非传说时装/武器。回「过 / 哪几张重画」。超过 24h 不回，按自检继续。
3. **收工**：门 A 绿、占位页没有真名、旧图没被覆盖。你点合并。不要升 4.6.1，不要交 APK。

## Agent 三刀

1. 仓库补「转职 / 融合」统一卡背「后续写入」（不进池、不写 28/24 个 id）
2. 换 18 张非传说拼卡为生成图
3. 时装 12 + 武器 12 的四段补满

做完停。28 转职、24 融合、镜头、升版：下一项目再开。
