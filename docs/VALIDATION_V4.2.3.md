# V4.2.3 验证说明

## 目标

战斗角色显示为清晰、稳定、不透明的正常 Q 版立绘；移动、攻击、冲刺和无敌状态不产生虚化、半透明重影或形变。武器方向与实际攻击方向保持一致。

## 验证状态

- 开发入口静态检查、内容包门禁和框架专项 7/7 通过。
- 完整浏览器流程 41/41 通过；三名角色均返回 `portraitMode: static`，无控制台错误和外部网络请求。
- 三名角色实战截图已人工检查：角色本体清晰不透明、无冲刺残影、无无敌透明闪烁、无常驻融合光环。
- 枪械和刀剑仍由独立武器层随目标方向旋转，枪口与子弹共线；逻辑动画状态继续保留给攻击判定和自动化测试。
- 官方网页游戏客户端完成两轮动作、状态与截图导出；完整战斗画面由项目浏览器回归覆盖。
- Android 同步通过：15 项核心美术、116 项 UI、15 项角色动画、168 项服饰；离线 bundle SHA-256 为 `50C548B8A7C63268247E0A673084E607322FC75A6CEFC3BADF6ACED65D506AC3`。
- Android `assembleRelease lintRelease` 返回 0；APK versionCode 47 / versionName 4.2.3，v1/v2 签名通过。
- Android 15 模拟器覆盖安装成功，`MainActivity` 为前台 Activity，应用 PID 存在，`FATAL/Uncaught` 匹配为 0。
