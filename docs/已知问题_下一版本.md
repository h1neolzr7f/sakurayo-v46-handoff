# 《樱夜·尸潮》下一版本已知问题

1. 原始交接 ZIP 的实际 SHA-256 为 `62036F35F20C14930538532F5A9BA8E9FDD176B6AF8D771133987B67A0E3086E`，与随附校验文件的 `87255484CD7E0859BBAB99C5411A4E763D204154AA43656A8E76F882477395A3` 不一致；ZIP 全条目可读且包内源码哈希与包内清单一致。
2. 本轮优先完成可靠开发基线，职业图鉴可视化树、角色被动成长、三阶演出、四类专属敌人、极简 HUD 与完整局后复盘仍待后续迭代。
3. 测试模式是维护入口，不是玩家功能；只有 `?test=1` 才会暴露受控测试 API。
4. 开发错误面板只有 `?debug=1` 或 `?test=1` 时显示；普通模式仅保留浏览器控制台行为。
5. 旧 WebView 回退已做静态与 Chromium 验证，但尚未在低版本 Android WebView 真机验证。