# Findings

## 2026-07-10 交接与协议
- 桌面项目根：`C:\Users\tzzcomputer\Desktop\樱夜尸潮_V3.5_Codex续作`。
- 唯一代码基线：`src/index.html`；`release/` 仅能在完成后由当前源码生成，禁止作为源码读取/回填。
- 交接包实际 SHA-256：`62036F35F20C14930538532F5A9BA8E9FDD176B6AF8D771133987B67A0E3086E`。
- 提供校验文件期望 SHA-256：`87255484CD7E0859BBAB99C5411A4E763D204154AA43656A8E76F882477395A3`，不匹配。
- ZIP 结构检查：33 条目、0 个损坏条目；安全解压 24 个文件。
- 包内未提供 `cases/<slug>/state.json`，已初始化本项目 case 以满足持续交接。
- 知识库检索未命中 HTML5 游戏开发条目；唯一命中为 APK/Smali，不适配本项目，只采用通用证据闭环原则。

## 强制设计摘要
- 三角色、14 基础职业/28 分支/18 融合、三相飞升、四章/Boss、商店/成就/本地存档均不可破坏。
- 第一优先是可靠基线：可重复冒烟、区块注释、开发期全局错误显示。
- 当前内容优先级：职业图鉴/技能树、主要三阶演出、专属敌人、数据复盘、少量隐藏三职业超融合。

## 2026-07-10 基线完整性与工具
- 包内 `SHA256SUMS.txt` 对 `src/index.html` 的记录为 `8935343f4192f22ddadab542b6eae9a8294b096b6b3a6a9bf8c2f38fcd791b08`，当前文件实际值一致。
- 现有 `tools/static_check.py` 只做核心字符串存在检查并提取 JS，不能替代浏览器行为测试。
- 现有 `tests/` 只有三张回归截图，没有自动化脚本；需要补充可重复冒烟。
- `src/index.html` 当前没有命中 `window.render_game_to_text`、`window.advanceTime`、`window.onerror` 或 `unhandledrejection`，需由精确审计确认并补齐。

## 2026-07-10 首次浏览器基线
- develop-web-game 客户端首次运行前缺少 Chromium v1217，已用其自身 Playwright CLI 安装匹配 Chromium/Headless Shell。
- PowerShell 原生参数内联 JSON 和 BOM 文件分别导致两次 JSON 解析失败；最终固定为 `tests/actions/*.json` 且用无 BOM UTF-8 写入，测试成功。
- `file:///.../src/index.html` 已由 Playwright 成功加载且未生成 console/page error 文件。
- 客户端输出 `tests/artifacts/baseline-menu/shot-0.png`。人工检查发现截图只有深色网格/樱花画布背景，没有主菜单 DOM；原因是技能客户端优先截取最大 canvas，而本项目主菜单是 canvas 上方 DOM。后续自动化必须同时做整页截图/DOM 断言，不能仅凭 canvas 截图判断主菜单。

## 2026-07-10 可靠基线实现
- `src/index.html` 现包含两个内联脚本：独立错误边界先于主 IIFE 注册；主游戏仍为单文件、无外部资源。
- 正常 URL 不暴露 `__SAKURAYO_TEST__`；仅 `?test=1` 启用手动时钟和受控状态钩子。`render_game_to_text` 为只读状态输出。
- 所有真实游戏循环调度统一经过 `scheduleLoop()`；测试模式不启动真实 RAF，`advanceTime(ms)` 以 60Hz 固定步推进，避免竞态。
- Boss 阶段测试使用真实 HP 阈值和 `fixedUpdate` 路径，75%/50%/25% 依次进入 2/3/4 阶。
- 浏览器冒烟报告和全页面截图位于 `tests/artifacts/smoke/`；静态抽取位于 `tests/artifacts/static/`。
- 当前修改后源码 SHA-256：`0CE75E3EB81B1A922C9A7183E1FE6356ADD89C8E164CF59B7CAFA2335EEA3AEE`（后续文档修改不影响源码；若源码再改需重算）。

- 最终源码/发布 SHA-256：`0CE75E3EB81B1A922C9A7183E1FE6356ADD89C8E164CF59B7CAFA2335EEA3AEE`；字节数：1471301。

## 2026-07-10 本轮续作
- 当前会话已提供内置 `image_gen`，可直接生成项目绑定素材，不需要 CLI 或本机 API key。
- 知识库对“HTML5 Canvas 游戏美术资源接入”无适配条目；仅返回 WebView 安全条目。可用结论是继续保持 APK 完全离线、避免不必要的 JS bridge 与远程加载。
- 参考图视觉特征：清爽日系二次元全身立绘、偏 Q 但非大头比例、细黑线稿、平涂加柔和赛璐璐阴影、暖黄纯色背景、黑白主色与少量红色点缀、猫耳/尾巴、金色大眼、现代制服感服装与日式刀剑；整体可爱但带战斗反差。
- 项目素材应继承上述“线稿、比例、赛璐璐光影、黑白红配色和可爱×末世反差”，但为三名既有原创角色保留各自武器、身份色和服装差异，不复制参考图具体人物。
- 第二张游戏构图参考图为竖屏肉鸽战斗＋深紫金属框 UI：角色/敌人需在手机尺度下有强轮廓，HUD 使用紫黑底、粉色强调、蓝绿橙品质色。图中伪文字不可复用，项目继续由 HTML/CSS/Canvas 渲染真实中文。
- 现有 `atlas_characters` 采用 3×3 图集（头像/战斗精灵/对话立绘 × 三角色），`atlas_branding` 采用 2×2；两者适合作为第一批内置 Image2 生成任务。UI 框体、按钮和文字仍应代码原生绘制，只将角色头像、技能图标和品牌图接入位图资源。

### 复用检查（外部内容，仅作参考）
- `Spritesheet.js` 和 CreateJS/EaselJS 都提供成熟的 Canvas sprite sheet API；web.dev 也有小型 AssetManager 参考实现。
- 本项目已经有 1.47 MB 单文件游戏、确定性测试钩子和 Android WebView 壳，迁移到 EaselJS/完整引擎会扩大回归面；Spritesheet.js 等旧库还会增加离线依赖与兼容风险。
- 决策：不引入第三方框架。仅参考“小型预加载器＋Canvas drawImage 回退”的模式，在现有 IIFE 内实现项目专用的图片资源表；图片加载失败时保留原始矢量/Canvas 角色与 UI，确保 APK 仍可玩。

### 当前 UI/战斗视觉基线
- Android 菜单已经有成熟的紫黑霓虹＋樱花主视觉、中文标题、紫粉渐变主按钮和六个底部功能按钮；整体方向与参考图兼容，主要缺口是角色选择区在截图中出现空白占位，角色资产接入需优先修复这里。
- 战斗 HUD 信息完整且触控可见，但玩家和敌人仍是简单几何/卡通 Canvas 图形，和菜单立绘质量断层明显。第一批生成应优先三名角色的 battle 精灵，并让 HUD 头像/角色卡复用同一身份资产。
- 现阶段不应重做整套 UI 布局；应保留已验证的触控尺寸与信息层级，只统一框体线条、渐变、樱花角饰、按钮图标与角色卡表现。

### 内置 Image2 首批素材 QA
- Aya 与 Rion 的头像、战斗精灵、对话立绘身份一致，透明角、比例和裁切正常；Sayo 的头像与对话立绘正常。
- Sayo 的 battle 单元右侧出现一块来自相邻单元的孤立细条，说明生成图没有完全遵守“不跨格”；不能直接接入。应在切格后按 alpha 连通域丢弃相对主主体过小的孤岛，再重新生成预览验证。
- 六枚导航图标构图清晰、无文字，3×2 顺序与“关卡/天赋/剧情/飞升/成就/商店”一致；不透明深紫底适合直接替换底部 emoji。
- 角色抠图边缘总体干净，发丝没有明显绿色硬边；当前软蒙版＋despill 可继续使用。
- Sayo 三格采用 50% 主连通域阈值重切后，右侧细条与边界色线已移除；最终 9 个角色 WebP 和 6 个导航 WebP 均通过人工联系表检查。
- 运行时采用 15 项核心资源表预加载：Android 使用 `game/art`，桌面源码使用 `../android-app/app/src/main/assets/game/art`；任一角色图片失败时回退原 SVG/Canvas。
- 全页截图确认：菜单三张角色卡、顶部头像、六枚导航图标均正确；三角色 battle 精灵在 430×932 战斗画面中保持身份和武器可辨。最终仅保留廉价身份色 halo；Android 模拟器自动质量曾降到 16fps，因此移除每帧图片 shadowBlur，避免为单个玩家精灵增加不必要的 GPU/Canvas 开销。

## 2026-07-11 剩余 UI
- 清单中未生成且属于 UI 的任务为：`atlas_progression_a/b` 64 项、`atlas_skills_a/b` 48 项、`atlas_branding` 4 项，总计 116 个 WebP。
- 知识库仍无 HTML5 游戏 UI 图集接入资料；唯一 WebView 命中偏安全测试。继续采用离线相对路径、无网络权限、无 JS native bridge 的既有结构。
- 代码/manifest 集合已核对：64 个成长图标与 48 个升级图标 ID 完全对齐，无 missing/extra。职业动态 ID `career:school:branch:rank` 必须取 branch 作为图片路径。
- 接入点为 `renderAscensionGuide()` 和 `renderChoices()`；112 项采用按需懒加载，不能加入启动期 `CORE_ART`，否则会拖慢 Android 冷启动。
- branding 四项当前无引用；menu_emblem 可非阻塞装饰，achievement_master 进入成就总进度卡，loading_art 仅作非阻塞启动层，app_icon 需派生 Android mipmap/adaptive icon。

## 2026-07-11 Q 版比例、碰撞与成熟产品对标

- 当前 512 方形关键帧主体 alpha 高约 488px，以 128×128 绁制时可见高度约 122px，占 430px 短边约 28.4%，明显过大，会遮挡弹道、敌人和脚底碰撞信息。
- 官方商店截图的保守构图推断：Vampire Survivors 玩家约短边 6–9%，Survivor.io 约 11–15%，20 Minutes Till Dawn 约 9–13%，Brotato 约 14–18%；本项目因细线二次元角色需要适度放大，但应将战斗绘制框降至 96×96 左右，而不是继续保留 128×128。
- 竞品共同点不是依靠脸部细节，而是依靠 Q 版头身块面、身份色、高对比轮廓、地面锚点和攻击轨迹。Q 版资产应限制为约 2.25 头身、3 个主明度块加 1 个身份色，并保留清晰武器轮廓。
- 当前玩家碰撞 `P.r=17`、视觉高度约122px，碰撞直径仅视觉高度28%；缩小到约92px后若保持17会变成37%。本轮采用 `P.r=16` 折中，使碰撞直径约视觉高度35%，仍符合“视觉体积大、实际碰撞小”的弹幕宽容原则。
- Q 版绘制以脚底为锚：96×96 方图建议 rect `(-48,-78,96,96)`，视觉底部为 `P.y+18`；影子保持在 `P.y+20~22`。角色图像尺寸与圆形碰撞必须解耦。
- 当前凛音存在空挥区间：先以178搜索目标，但普通斩半径只有128；绫第四枪也用固定115搜索而非 `radius+e.r`。应统一用实际圆半径 `distance < slashRadius + enemy.r` 判定是否触发近战。
- 普通枪弹应从 Q 版上半身枪口出生，而不是 `P.x/P.y` 腹部中心；径向技能仍从角色中心释放。敌弹也应从敌人半径外缘出生，避免弹体藏在精灵内部。
- 当前命中闪烁0.1秒、伤害数字0.55秒已处于成熟产品常见的可读范围，无需为了“更炫”延长遮挡。
- 当前四章/Boss/升级/压强导演不是空壳，但角色专属剧情多数只替换事件标题/正文；选项、flag、结局与大部分吐槽仍复用小夜路线。现有17项 smoke 主要覆盖第一章，不能声称四章剧情已经完整验证。
- 本轮剧情可玩性门禁应覆盖：3角色×4章开场、8个中途事件、四Boss 75/50/25阶段、高爆发不跳阶段、吐槽/剧情层互斥，以及压强30秒峰谷是否可感知。
- 三角色 Q 版五状态图集已生成并切分。人工联系表确认小夜、绫、凛音均保持身份色、发型、服装和武器，约2.25头身，动作顺序正确。
- 初次沿用旧成人比例图的重叠裁切框，导致凛音相邻动作残片串格；改为严格等宽切格后消失。绫冲刺主体跨越默认边界，单独使用 `[1580,0,2172,724]` 裁切后恢复完整身体且无邻格残片。
- Q 版 WebP 继续保存为512方形透明画布，运行时只以96逻辑像素绘制；这样资产可保留高质量边缘，又不会恢复到过大的屏占比。
# V3.6 主神空间与武器方向

- 旧枪弹本身可以 360 度飞行，但武器素材只左右翻转；现使用同一 weaponAngle 绘制枪身、枪口和弹道。
- 旧太刀调用全圆 aoe，导致身后敌人受伤；现使用距离、敌人半径和扇形夹角联合判定。
- 直接把主神空间作为 ST 第五章会触发四章数组越界并污染 done/story/endings；现使用独立 runMode，战斗机制复用终章但存档和结算分流。
- 主神成长必须分两层：P.up/careers/fusion/form 每轮 reset；save.mainGod 兑换跨轮回保存且只在主神模式应用。
- 原吐槽基表 133 个触发包均以小夜人格编写，名字替换不能形成角色差异；现新增角色语义触发表，每人 8 个高价值彩蛋。
- Android 首次冷启截图在 5 秒时仍为黑色加载页，15 秒后完整菜单出现且无崩溃日志；属于冷启动资源解码时间，不计为启动失败。
- 旧封面把标题、货币、角色卡和按钮全部烘焙进一张图，既会与真实 DOM 重叠，也会让旧数据和错字无法维护。V3.6 改为“无字主视觉背景＋真实界面层”，封面和菜单信息职责分离。
- 新封面与战斗 Q 版不是同一比例用途：封面保留精细宣传表现，战斗继续使用约 2.25 头身 Q 版和独立小碰撞体，避免宣传图比例污染战斗可读性。

## 2026-07-12 V3.8 UI 与剧情审计

- 战斗品质断层的根因已确认：`enemyDraw()` 仍以纯色圆、帽檐和双眼绘制全部 11 类敌人；Boss 也共用圆形主体，仅叠加章节符号。角色已是高质量 Q 版位图，因此敌人与 Boss 是当前最显眼的占位层。
- 顶部 HUD 同时常驻生命、护盾、经验、时间、击破、章节、稳定度、压强、任务和暂停；信息完整但在 430 宽手机上字号过小。Boss 离开画面时没有屏外箭头，阶段四截图中只看到预警圈而看不到 Boss。
- 场地预警已经具备 1.1–1.55 秒倒计时和文字形状，但所有圆圈同样使用低透明描边，密集时缺少“预警/锁定/爆发”三个明确视觉阶段。
- 升级卡已有技能图标、稀有度和推荐原因，但缺少“当前值→升级后”、职业核心进度、冲突风险的固定位置；玩家仍需阅读描述自行推断。
- 职业页虽然信息完整并可搜索，但仍是纵向长卡列表，不具备当前路线聚焦和节点关系；主神商店也缺少分组标签与下一等级差值。
- 基础 `endingData()` 仍以小夜 flag 为中心；绫与凛音虽有独立四章开场和结局补丁，但 Boss 阶段对白仅替换一行，中途事件存在大量共享骨架。V3.8 应给每名角色设置明确的“罪责/自主权/继承”主题变量，并在四章逐步回收。
- 三条叙事主题冻结：小夜面对“复制人格是否有选择死亡与活着的权利”；绫面对“无知是否免除签字者责任，以及妹妹是否能拒绝被救回”；凛音面对“继承记忆是否等同继承一个人，以及传统是否允许后来者改写”。

## 2026-07-12 V3.8 实施结论
- 敌人和 Boss 的正式 Q 版素材可在不改变碰撞几何的前提下接入；视觉尺寸由 `e.r` 派生，命中仍使用原圆形碰撞，避免“图变大、判定也变大”。
- 12 个敌人轮廓在手机缩略图下可区分：速度型依靠前倾姿态、坦克依靠体块、远程依靠符纸、爆炸依靠圆形炸弹、克制怪依靠盾/天线/面具/幽灵/祭司/锁链。
- 四章 Boss 的阶段素材按 `stageN_phaseN` 命名，转阶段直接换图；主神监察者暂时保留程序化几何身份层，以避免把普通终章镜姬误当主神本体。
- 升级数值条的精确映射目前覆盖伤害、射速、生命、移速、暴击、穿透和多重弹；其余技能显示技能等级变化，避免伪造不准确的数值。
- Android 冷启动截图可能在首个 2 秒只得到纯黑 WebView 背景；继续等待后菜单正常，日志无 FATAL/JS Uncaught，属于资源解码窗口而非启动失败。
- 验证期间两次工具路径错误：首次误用不存在的 `tools/static_check.mjs`，改用实际 `tools/static_check.py`；首次按默认 SDK 路径寻找 build-tools 失败，依据 `local.properties` 改用 `E:\Android\android-sdk`。
- `aapt` 无法直接读取含中文的 APK 路径，复制到 ASCII 临时路径后成功确认 versionCode 38/versionName 3.8.0；`apksigner` 在原路径仍成功验证 v1/v2 和原证书摘要。
- 第二次模拟器截图首次 `adb pull` 使用了错误的相对输出目录，改为 `..\tests\artifacts\android` 后成功；不影响 APK 或设备状态。
- 更新交付包时首次把两个目标路径数组传给 `Copy-Item -Destination`，PowerShell 不支持该写法；改为逐目标复制后重新压缩，并按最终文件重算 SHA-256。
# V3.9 审计结论（2026-07-12）

1. 现有 82/56 动态敌人上限能够保护手机帧率，但主神模式额外 +10 会放大视觉遮挡，应把“爽感”更多交给经验倍率、连锁清屏和精英奖励，而不是继续堆实体。
2. 三名角色的武器方向和碰撞已经成立，但主动决策仍偏少；冲刺适合作为统一的完美闪避入口，凛音可在同一窗口获得太刀格挡反击，避免新增按钮挤压手机界面。
3. Boss 已有四阶段和场地预警，但阶段差异主要是弹幕数量；应增加可被理解的规则目标，并在 HUD 显示当前规则。
4. 吐槽/播报已有队列互斥，下一步重点不是增加频率，而是角色差异、冷却和剧情回顾。
5. 主神空间已有永久商店、随机契约和挑战，缺口是带代价的永久遗物、随机轮回标签与更明确的局内/永久边界。
6. 存档导入只检查根字段，需加入版本、数值边界、数组长度和校验摘要，避免损坏存档导致黑屏或异常数值。

## V4.0 设计决策

1. 内测后门只在 `?beta=1` 或 `?test=1` 暴露，正式离线包默认不可见；所有数据仍写入本地，不加入联网遥测。
2. 初始强化采用“购买等级 + 装备一种核心 + 章节同步倍率”，避免普通天赋、服饰倾向和主神永久强化三套系统互相混淆。
3. 丧尸诱饵的爽感来源为额外刷怪脉冲、延长普通波次和经验加成；实体仍受 82/56 动态上限约束，不能用无限堆怪换爽感。
4. 排除符只过滤职业学校技能，不过滤通用伤害/生命/移速，也不阻止已经形成职业后的转职与飞升，避免升级池枯竭。
5. “仅凭初始强化通关”实现为可主动开启的孤证试炼：升级时不弹三选一，角色仍会提升等级但不获得局内能力；无伤通关解锁隐藏内容。
6. 冲突升级必须在卡面显示红色代价并有独立吐槽；其价值可在特定构筑中成立，但盲选应明显弱于规划升级。

## V4.0 首轮平衡数据

- 满级夜樱火控核心在第四章获得 1.54 倍章节同步系数；实际基础伤害提升约 92.4%，射速提升约 23.1%，并增加约 9.2% 暴击。
- 在不取得任何局内强化、无受伤代理条件下，小夜终章完整流程 4:19 通关，击破 386、精英 40、等级 13；Boss 四阶段均实际进入，说明初始核心输出下限足以覆盖纯初始挑战。
- 该结果只证明“高玩无伤代理的伤害时限可行”，不代表普通玩家无需局内构筑；商店卡面应继续把孤证试炼标为挑战模式。
- 三级诱饵 7 秒测试中至少触发 2 次额外生成，且总实体未突破动态预算；不能用屏幕存活怪数量衡量诱饵生成，因为角色会同步击杀。
- 排除符 40 组升级池抽样未出现被排除的 gun/mech 普通职业技能；通用与职业进阶仍可出现，升级池没有枯竭。

## V4.0 最终校准

- 为避免随机波次污染角色比较，主回归使用固定 LCG 种子，并在每名角色孤证试炼前重置为同一 seed。
- 保持绫的手枪伤害 -18%、射击间隔 +28% 不变，仅把每第四枪月切从 105/1.35 初始基线校准为 125 范围、2.05 倍近战伤害。
- 同条件终章结果：小夜 244 秒、绫 230 秒、凛音 204 秒；最大/最小为 1.196，低于 1.25 门禁。
- 凛音仍最快是合理风险回报：必须进入 128/176 近战扇区；小夜最安全且射程最高；绫位于两者之间。
- `web_game_playwright_client` 仍只截取最大 Canvas，因此菜单 DOM 截图为空背景；最终视觉验收采用 browser_smoke 的整页截图，状态 JSON 同时确认 shop40/balance 字段。
# V4.0 新融合设计结论

1. 扩充融合数量不能只增加名称或全伤害倍率；每个新融合至少需要一个可自动测试的独立战斗事件，才能让组合改变实际操作与构筑循环。
2. 六个新组合覆盖召唤、持续伤害、枪械连射、冲刺、主动治疗和法术吸血六个缺口，并通过治疗、护盾、射程或天敌代价避免无条件替代旧融合。
3. 融合条件可以读取永久商店装备作为辅助门槛，但装备只能提高成型可控性，不能直接跳过两个基础职业成型条件。
4. 新融合仍受既有实体上限和伤害统计限流；周期召唤通过伤害事件而非无限创建常驻实体实现，防止怪海场景叠加卡顿。
5. 初次回归暴露六个新融合图标路径 404；已补齐离线别名资源并再次验证正常模式零启动错误、零外部网络请求。

# V4.1 架构审计与决策

1. 现有 `src/index.html` 为 6118 行、两个内联脚本；战斗、数据、UI 和存档均位于第二个 IIFE，直接拆变量会扩大闭包与初始化顺序风险。
2. 最稳的 seam 不是立刻拆战斗函数，而是在 IIFE 前建立全局内容包注册表，在 IIFE 内以一个兼容适配器读取已验证数据；核心战斗继续作为单一实现。
3. MDN 资料表明经典脚本可以按声明顺序执行；模块脚本依赖 CORS。Android 官方指出 `file://` 是不透明源并推荐 `WebViewAssetLoader`。因此开发态使用经典脚本，不使用 ES Modules/JSON fetch；Android 改用 `appassets.androidplatform.net` 本地 HTTPS 映射。
4. 内容包协议应只接受可序列化数据和有限谓词名，第三方不获得任意核心函数引用；官方包可通过受保护事件接口订阅生命周期，但每个监听器必须独立 try/catch。
5. 扩展发现不能依赖浏览器扫描目录；由 `src/content/extensions.manifest.js` 显式列出包，构建器按同一清单生成开发脚本标签和发布单入口。
6. 存档仍由本体持有 `sakurayoV3`。扩展状态集中放入 `save.extensions[packId]`，每包声明版本、默认值和逐版本迁移；迁移失败保留该包原始快照并仅禁用该包。
7. 服饰和成就是低风险的首个垂直切片：两者已有数组/对象式渲染入口，可证明“加文件/加数据包”成立，而无需触碰 `update()`、`draw()` 或刷怪数值。

### 外部参考（仅用于决策）

- MDN `<script>`：经典 `defer` 脚本按出现顺序执行，适合无模块/CORS依赖的旧 WebView 兼容加载。
- Android Developers `WebViewAssetLoader`：建议用 `https://appassets.androidplatform.net/assets/...` 加载 APK 内资源，并关闭 file URL 跨域访问。
- 决策：不引入 Rollup/SystemJS/第三方插件框架；项目规模和离线目标更适合约 200 行的专用注册表加确定性内联构建器。

## Image2 探索地图与通用探索 seam

1. 四张生成图均为 1536×1024 三分之二俯视构图；压缩到 1280×853 WebP 后单张约 219–242 KB，适合 APK 离线加载。
2. 竖屏若把整张 3:2 地图 contain 会浪费大量高度；采用世界图 cover 加角色跟随镜头，可在 430×932 满屏显示并通过移动查看整幅地图。
3. 探索不复用战斗实体数组或主循环。独立 RAF、键位/摇杆和存档段避免地图扩展损坏战斗帧循环，符合失败隔离目标。
4. 地图图片失败时使用程序化紫黑渐变，节点和角色仍可移动收集；资源失败不会阻断退出或本体菜单。
5. 奖励限制为少量樱花币和档案碎片，不修改刷怪、职业与 Boss 数值。
